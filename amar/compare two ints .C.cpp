//Program in c to compare to integers
#include<stdio.h>
int main()
{
	int x,y,z;
	printf("enter two integers");
	scanf("%d%d",&x,&y);
	if(x>y);
	printf("%d is greater than %d",x,y);
	 if(y>x);
	{printf("%d is greater than %d",x);}
	 {printf("both the entered values are equal");
	}
	return 0;
}
